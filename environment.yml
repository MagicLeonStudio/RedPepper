name: redpepper
channels:
  - conda-forge
  - defaults
dependencies:
  # Python
  - python>=3.10,<3.13

  # --- Backend ---
  - fastapi>=0.110
  - uvicorn>=0.27
  - sqlalchemy>=2.0
  - pydantic>=2.0
  - httpx>=0.27
  - cryptography>=42.0
  - pyyaml>=6.0
  - python-multipart>=0.0.9

  # --- Frontend ---
  - pyqt>=6.6

  # --- AI (OpenAI compatible SDK) ---
  - openai>=1.0
  - aiohttp>=3.9

  # --- Dev / Testing ---
  - pytest>=8.0
  - pytest-asyncio>=0.23
  - black>=24.0
  - ruff>=0.4

  # --- Packaging ---
  - pyinstaller>=6.0
